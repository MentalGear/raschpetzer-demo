node_modules

# Output (non-anchored so they match per-app dirs too, e.g. apps/photos/build)
.output
.vercel
.netlify
.wrangler
.svelte-kit/
build/
dev-dist/

# Playwright (generated test artifacts: screenshots, traces, reports)
test-results/
playwright-report/
blob-report/
.playwright/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Log files
*.log

*storybook.log
storybook-static

# Agent scratch space (throwaway)
/.agents/scratch/

# Isolated git worktrees created for background subagent runs (transient, per-session)
/.claude/worktrees/
