// Wikipedia is a client-side reading/editing app (runes store, dynamic routes,
// in-memory edit session) rendered as an SPA, like Photos and Notes.
export const ssr = false
export const prerender = false
